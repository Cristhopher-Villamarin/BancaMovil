import 'package:flutter/material.dart';

class PagoServiciosVista extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pago a Servicios')),
      body: Center(child: Text('Página de Pago a Servicios')),
    );
  }
}